import React, { useState } from 'react'
import ReactDOM from 'react-dom'

const domTarget = document.getElementById('dashboard')

const Dashboard = () => {
  return (
    <div>Hi</div>
  )
}

ReactDOM.render(<Dashboard/>, domTarget)